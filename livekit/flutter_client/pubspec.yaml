name: livekit_calling
description: LiveKit-based video calling client for Flutter.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  livekit_client: ^2.3.0
  http: ^1.2.0
